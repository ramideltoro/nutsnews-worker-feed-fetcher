{
  "name": "@ramideltoro/nutsnews-worker-feed-fetcher",
  "version": "0.1.0",
  "description": "Deployable worker-uplift feed fetcher service shell for NutsNews.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ramideltoro/nutsnews-worker-feed-fetcher.git"
  },
  "bugs": {
    "url": "https://github.com/ramideltoro/nutsnews-worker-feed-fetcher/issues"
  },
  "homepage": "https://github.com/ramideltoro/nutsnews-worker-feed-fetcher#readme",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "clean": "rm -rf dist coverage *.tgz *.sbom.json",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "sbom": "npm sbom --sbom-format=cyclonedx > nutsnews-worker-feed-fetcher.sbom.json",
    "audit:prod": "npm audit --omit=dev --audit-level=high",
    "container:build": "docker build --pull=false --secret id=npm_token,env=NODE_AUTH_TOKEN -t nutsnews-worker-feed-fetcher:local .",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run test:integration && npm run build && npm run sbom && npm run audit:prod"
  },
  "dependencies": {
    "@ramideltoro/nutsnews-worker-contracts": "0.3.1",
    "@ramideltoro/nutsnews-worker-runtime": "0.5.0",
    "amqplib": "^2.0.1",
    "fast-xml-parser": "^5.10.1",
    "pg": "^8.22.0",
    "undici": "^8.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@types/amqplib": "^0.10.8",
    "@types/node": "^24.1.0",
    "@types/pg": "^8.20.3",
    "eslint": "^9.32.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.38.0",
    "vitest": "^3.2.4"
  },
  "packageManager": "npm@11.13.0",
  "engines": {
    "node": ">=24"
  }
}
